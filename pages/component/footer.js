import footer from "./../../styles/Footer.module.css";
const Footer = () => {
  return (
    <footer className={footer.footer}>
      <p class={footer.title}>v-project-app</p>
    </footer>
  );
};

export default Footer;
